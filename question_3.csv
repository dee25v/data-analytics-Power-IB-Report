\copy (
SELECT 
    s.store_type,
    SUM(o.product_quantity * p.sale_price) AS total_revenue
FROM orders o
JOIN dim_products p
    ON o.product_code = p.product_code
JOIN dim_stores s
    ON o.store_code = s.store_code
WHERE s.country_code = 'DE'
  AND EXTRACT(YEAR FROM TO_DATE(o.order_date, 'DD/MM/YYYY')) = 2022
GROUP BY s.store_type
ORDER BY total_revenue DESC
LIMIT 1
) TO '/Users/deepavalgi/Downloads/question_3.csv' CSV HEADER